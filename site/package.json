{
  "name": "vue2-components-site",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:preview": "vite build --mode preview",
    "build:intranet": "vite build --mode intranet",
    "routes": "node scripts/genrateRoutes.mjs"
  },
  "dependencies": {
    "element-ui": "^2.15.9"
  },
  "devDependencies": {
    "unplugin-vue2-script-setup": "^0.11.2"
  }
}
